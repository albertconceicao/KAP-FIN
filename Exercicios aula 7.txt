------Aula 07------

Estruturas de Repetição:

WHILE - Aplica o código enquanto 
	while 

> SELECT "Matemática":
		-> Then 
		-> float soma = P1 + P2 + P3 + P4
		-> float resultado = soma/CD
		-> float (Resultado >=Media)
			-> Then statusAluno = "Aprovado"
		-> Else
			-> statusAluno = "Reprovado"

		->while(resultado < Media):
		-> float resultadoAno = resultado
		-> pr = aplicar mais uma prova
		-> resultado = resultado + pr /2



Aula 08/04/2021

Exercícios

= Receber o valor
== Comparar valor

Qual a diferença entre o return ou console.log?
O return testa o código inteiro
O console.log testa a somente a notação;

function calculaMedia (A, B, C){
	if (A>= B + C){
		console.log( "Não existe triângulo")
	}
	if(A**2 = B**2 + C**2){
		return "Triangulo Retangulo"
	}
	if(A < B)


—>for(int i = 1; i <= 500; i++)
—>escreva("Digite um numero")
—>leia(numero)
       —>if(numero > maior)
		maior = numero
		
       —>if(numero < menor)
		menor = numero
		
	—>soma = soma + numero
                 
	—>media = soma/500
